import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const userGroupImporterSelectors = importerSelectors(
  'userGroup.importer',
);

export default userGroupImporterSelectors;
