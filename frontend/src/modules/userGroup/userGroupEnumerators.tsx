import colors from 'src/mui/assets/theme/base/colors';

const userGroupEnumerators = {
  type: ['Default', 'Office', 'LineOfBusiness'],
  typeColor: [
    colors.todo.main,
    colors.inprogress.main,
    colors.success.main,
  ],
};

export default userGroupEnumerators;
