import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const highlightImporterSelectors = importerSelectors(
  'highlight.importer',
);

export default highlightImporterSelectors;
