import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const programTemplateImporterSelectors = importerSelectors(
  'programTemplate.importer',
);

export default programTemplateImporterSelectors;
