import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const projectTypeImporterSelectors = importerSelectors(
  'projectType.importer',
);

export default projectTypeImporterSelectors;
