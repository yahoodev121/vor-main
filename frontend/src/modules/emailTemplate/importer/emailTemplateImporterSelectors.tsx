import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const emailTemplateImporterSelectors = importerSelectors(
  'emailTemplate.importer',
);

export default emailTemplateImporterSelectors;
