import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const projectStatusImporterSelectors = importerSelectors(
  'projectStatus.importer',
);

export default projectStatusImporterSelectors;
