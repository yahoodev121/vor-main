import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const qaLibraryImporterSelectors = importerSelectors(
  'qaLibrary.importer',
);

export default qaLibraryImporterSelectors;
