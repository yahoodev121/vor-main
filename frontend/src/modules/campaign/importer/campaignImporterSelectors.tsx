import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const campaignImporterSelectors = importerSelectors(
  'campaign.importer',
);

export default campaignImporterSelectors;
